{% extends 'base.html.twig' %}

{% block title %}Produits index

{% endblock %}

{% block body %}
	<h1>Liste des produits</h1>

	<button id="add">
		<a href="{{ path('produits_new') }}">Ajouter un produit</a>
	</button>

	<table class="table">
		<thead>
			<tr>
				{# <th>Id</th> #}
				<th>Nom</th>
				<th>Reference</th>
				{# <th>Categorie</th> #}
				<th>Date_achat</th>
				{# <th>Lieu_achat</th> #}
				{# <th>Date_fin_garantie</th> #}
				<th>Prix</th>
				{# <th>Conseils</th> #}
				{# <th>Photo</th> #}
				{# <th>Manuel</th> #}
					<th>
				actions
			</th>
		</tr>
		</thead>
			<tbody>
				{% for produit in produits %}
					<tr>
					{# <td>{{ produit.id }}</td> #}
						<td>
					{{ produit.Nom }}
					</td>
						<td>
					{{ produit.Reference }}
					</td>
					{# <td>
																																																							{{ produit.Categorie }}
																																																							</td> #}
						<td>
					{{ produit.DateAchat ? produit.DateAchat|date('d/m/Y') : '' }}
					</td>
					{# <td>
																																																		{{ produit.LieuAchat }}
																																																		</td> #}
					{# <td>
																																																												{{ produit.DateFinGarantie ? produit.DateFinGarantie|date('d/m/Y') : '' }}
																																																												</td> #}
						<td>
					{{ produit.Prix }}
					</td>
					{# <td>{{ produit.Conseils }}</td> #}
					{# <td>{{ produit.Photo }}</td> #}
					{# <td>{{ produit.Manuel }}</td> #}
						<td>
							<a id="see" href="{{ path('produits_show', {'id': produit.id}) }}"> Voir le produit</a>
						<a id="modif" href="{{ path('produits_edit', {'id': produit.id}) }}">Modifier</a>
					</td>
				</tr>
			{% else %}
				<tr>
					<td colspan="12">no records found</td>
				</tr>
			{% endfor %}
		</tbody>
	</table>
	<button id="deco">
		<a href="{{ path('app_logout') }}">Déconnexion</a>
	</button>


{% endblock %}
