<form method="post" action="{{ path('produits_delete', {'id': produit.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer ce produit ?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ produit.id) }}">
	<button id="delete">Supprimer</button>
</form>
